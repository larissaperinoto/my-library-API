const booksController = require('./books.controller');
const userController = require('./user.controller');
const wishesController = require('./wishes.controller');

module.exports = {
  booksController,
  userController,
  wishesController,
};
